Vite + React + TypeScript scaffold with Tailwind configured.

To run:

cd react-app
npm install
npm run dev

Notes:
- Replace Google Maps API key by adding the script tag to `index.html` or use a loader.
- The components keep original class names so existing SCSS can be migrated incrementally.